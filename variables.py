# by default input will be in string format ony
# so we need to convert into integer type
# num = int(input())
# print(type(num))

# we can unpack the values we want for an variable
# print(range(2))

id1, id2, id3 = range(3)
print(id1)
print(id2)